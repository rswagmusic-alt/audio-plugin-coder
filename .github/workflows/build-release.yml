name: Build & Publish Plugin

on:
  workflow_dispatch:
    inputs:
      plugin:
        description: 'Which plugin to build?'
        required: true
        type: choice
        options:
          - CloudWash
          - gnarly2
          - nf_gnarly
          - VictorRSwagVocalComp
          - all
      platforms:
        description: 'Which platforms to build?'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - windows
          - macos
          - linux
      version:
        description: 'Version number for the Windows installer (e.g. 1.0.0)'
        required: false
        default: '1.0.0'
      macos_vst3:
        description: 'macOS: Build VST3?'
        required: false
        default: true
        type: boolean
      macos_au:
        description: 'macOS: Build AU?'
        required: false
        default: true
        type: boolean
      macos_standalone:
        description: 'macOS: Build Standalone?'
        required: false
        default: false
        type: boolean

env:
  BUILD_TYPE: Release

jobs:
  # ============================================
  # RESOLVE PLUGIN MATRIX
  # ============================================
  setup:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Set plugin matrix
        id: set-matrix
        run: |
          if [ "${{ inputs.plugin }}" == "all" ]; then
            echo 'matrix={"plugin":["CloudWash","gnarly2","nf_gnarly"]}' >> $GITHUB_OUTPUT
          else
            echo 'matrix={"plugin":["${{ inputs.plugin }}"]}' >> $GITHUB_OUTPUT
          fi

  # ============================================
  # WINDOWS BUILD
  # ============================================
  build-windows:
    needs: setup
    if: inputs.platforms == 'all' || inputs.platforms == 'windows'
    runs-on: windows-latest
    strategy:
      matrix: ${{ fromJson(needs.setup.outputs.matrix) }}
      fail-fast: false
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1.13.0

      - name: Configure CMake
        # Don't hardcode a "Visual Studio <N> <year>" generator name: the
        # windows-latest runner image's bundled VS version changes over time
        # (e.g. it moved past "Visual Studio 17 2022"), and a stale hardcoded
        # name makes CMake fail with "could not find any instance of Visual
        # Studio" even though a newer VS is installed. Omitting -G lets CMake
        # auto-detect the newest installed VS generator.
        run: cmake -S . -B build -A x64

      - name: Build VST3
        run: cmake --build build --config Release --target "${{ matrix.plugin }}_VST3"

      - name: Build Standalone
        run: cmake --build build --config Release --target "${{ matrix.plugin }}_Standalone"

      - name: Install Inno Setup
        run: choco install innosetup --no-progress -y

      - name: Create Windows installer (.exe)
        shell: pwsh
        # Non-fatal: a packaging hiccup shouldn't lose the raw VST3/Standalone
        # artifacts collected below.
        continue-on-error: true
        run: |
          .\scripts\installer\create-windows-installer.ps1 `
            -PluginName "${{ matrix.plugin }}" `
            -Version "${{ inputs.version || '1.0.0' }}"

      - name: Collect artifacts
        shell: bash
        run: |
          mkdir -p release
          find build -name "*.vst3" -exec cp -R {} release/ \;
          find build -name "*.exe" -not -path "*/CMakeFiles/*" -not -path "*/build/installer/*" -exec cp {} release/ \;
          echo "Collected:" && ls release/

      - name: Upload artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: windows-${{ matrix.plugin }}
          path: release/
          if-no-files-found: error
          retention-days: 30

  # ============================================
  # MACOS BUILD
  # ============================================
  build-macos:
    needs: setup
    if: inputs.platforms == 'all' || inputs.platforms == 'macos'
    runs-on: macos-latest
    strategy:
      matrix: ${{ fromJson(needs.setup.outputs.matrix) }}
      fail-fast: false
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Install Dependencies
        run: brew install cmake || true

      - name: Configure CMake
        run: |
          cmake -S . -B build -G Xcode \
            -DCMAKE_OSX_ARCHITECTURES="x86_64;arm64" \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=10.13

      - name: Build VST3
        if: inputs.macos_vst3
        run: cmake --build build --config Release --target "${{ matrix.plugin }}_VST3"

      - name: Build AU
        if: inputs.macos_au
        run: cmake --build build --config Release --target "${{ matrix.plugin }}_AU"

      - name: Build Standalone
        if: inputs.macos_standalone
        run: cmake --build build --config Release --target "${{ matrix.plugin }}_Standalone"

      - name: Collect artifacts
        run: |
          mkdir -p release
          find build -name "*.vst3" -exec cp -R {} release/ \;
          find build -name "*.component" -exec cp -R {} release/ \;
          find build -name "*.app" -not -path "*/CMakeFiles/*" -exec cp -R {} release/ \;
          echo "Collected:" && ls release/

      - name: Upload artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: macos-${{ matrix.plugin }}
          path: release/
          if-no-files-found: error
          retention-days: 30

  # ============================================
  # LINUX BUILD
  # ============================================
  build-linux:
    needs: setup
    if: inputs.platforms == 'all' || inputs.platforms == 'linux'
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJson(needs.setup.outputs.matrix) }}
      fail-fast: false
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            libasound2-dev \
            libfreetype6-dev \
            libgl1-mesa-dev \
            libx11-dev \
            libxcomposite-dev \
            libxcursor-dev \
            libxext-dev \
            libxinerama-dev \
            libxrandr-dev \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libjack-jackd2-dev \
            xvfb

      - name: Configure CMake
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build VST3
        run: cmake --build build --target "${{ matrix.plugin }}_VST3"

      - name: Build LV2
        run: xvfb-run cmake --build build --target "${{ matrix.plugin }}_LV2"

      - name: Build Standalone
        run: xvfb-run cmake --build build --target "${{ matrix.plugin }}_Standalone"

      - name: Collect artifacts
        run: |
          mkdir -p release
          find build -name "*.vst3" -exec cp -R {} release/ \;
          find build -name "*.lv2" -exec cp -R {} release/ \;
          find build -name "${{ matrix.plugin }}" -type f -exec cp {} release/ \;
          echo "Collected:" && ls release/

      - name: Upload artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: linux-${{ matrix.plugin }}
          path: release/
          if-no-files-found: error
          retention-days: 30

